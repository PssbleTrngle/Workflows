- uses: actions/setup-java@v4
  with:
     java-version: {{ javaVersion }}
     distribution: temurin

- uses: gradle/actions/setup-gradle@v4
  with:
     cache-read-only: {{ var 'github.event_name != \'push\'' }}
