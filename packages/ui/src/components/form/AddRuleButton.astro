---
type NumberOrOperation = number | `++${string}`;

export type Props = { path: string; index?: NumberOrOperation };

const { path, index } = Astro.props;
---

<button
  hx-template="rule-input-template"
  hx-swap="outerHTML"
  hx-data={JSON.stringify({ path, index })}
>
  Add {path}
</button>
