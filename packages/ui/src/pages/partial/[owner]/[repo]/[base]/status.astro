---
import { createTopic } from "@pssbletrngle/workflows-shared/topic";
import { type RepoSearchWithBranch } from "@pssbletrngle/workflows-types";
import type { StatusResult } from "@pssbletrngle/workflows-types/metadata";
import StatusComponent from "../../../../../components/Status.astro";

export const partial = true;

const { api } = Astro.locals;

const data = Astro.params as RepoSearchWithBranch;

const { status, checks } = await api.request<StatusResult>(
  createTopic(data, "status"),
);
---

<StatusComponent status={status} checks={checks} subject={data} />
