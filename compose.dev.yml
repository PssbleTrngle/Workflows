name: workflows

services:
  app:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    container_name: workflows-server
    ports:
      - "8888:8888"
    env_file: apps/server/.env.local
    environment:
      REDIS_HOST: redis
      RABBITMQ_URL: amqp://rabbitmq:5672
    volumes:
      - ./apps/server/private-key.pem:/usr/private-key.pem
    depends_on:
      redis:
        condition: service_started
      rabbitmq:
        condition: service_started
